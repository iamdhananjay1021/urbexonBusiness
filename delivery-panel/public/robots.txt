# delivery.partner.urbexon.in — delivery partner portal. No public/
# marketing content lives here (rider recruitment content, if any, lives
# on the client app) — this origin is pure authenticated tooling.
# Blanket disallow; paired with the noindex meta tag in index.html and
# the X-Robots-Tag response header.

User-agent: *
Disallow: /
