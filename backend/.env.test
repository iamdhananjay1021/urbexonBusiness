# Test-only environment for `npm test` (backend/tests/*.test.js).
# Never used in production — dummy values only, safe to commit.
JWT_SECRET=test_only_jwt_secret_do_not_use_in_production_xxxxxxxx
MONGO_URI=mongodb://127.0.0.1:27017/urbexon_test
NODE_ENV=test
RAZORPAY_KEY_ID=test_key_id
RAZORPAY_KEY_SECRET=test_key_secret
CLOUDINARY_CLOUD_NAME=test
CLOUDINARY_API_KEY=test
CLOUDINARY_API_SECRET=test
